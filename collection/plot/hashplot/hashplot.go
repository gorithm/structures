package hashplot

// HashPlot implements a Go map in the back-end, but allows for extra functionality which cannot be
// achieved succinctly with the default map.
//
// HashPlot implements the Plot and Collection interfaces.
type HashPlot struct {
}
